# Example Pushover configuration for pushover_utils
# Save as ~/etc/pushover.env (or specify path when creating PushoverNotifier)

# Pushover API credentials (both exactly 30 characters)
PUSHOVER_TOKEN=abcdefghijklmnopqrstuvwxyz1234
PUSHOVER_USER=abcdefghijklmnopqrstuvwxyz5678

# How to get your credentials:
# 1. Sign up at https://pushover.net
# 2. Your User Key is shown on the main page (30 characters)
# 3. Create a new application at https://pushover.net/apps/build
# 4. Your Application Token will be generated (30 characters)
# 5. Replace the example values above with your real credentials

# Priority levels you can use:
# -2: Lowest (no notification)
# -1: Low (quiet notification)
#  0: Normal (default)
#  1: High (bypass quiet hours)  
#  2: Emergency (requires acknowledgment)